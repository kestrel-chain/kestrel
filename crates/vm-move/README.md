# vm-move

Move VM hosting and Kestrel object/resource primitives. It verifies module publication and entry calls, maps Move change sets into atomic object transitions, and binds signer arguments to transaction senders.
