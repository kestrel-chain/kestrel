# state

Deterministic Merkle state for Kestrel objects. It owns version-checked object transitions, epoch rent charging, and the separate active/expired state commitments used by sequential execution.
